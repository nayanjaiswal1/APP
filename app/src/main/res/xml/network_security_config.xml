<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
       Strict Network Security Configuration:
       1. Enforces TLS across all network connections by default (no cleartext HTTP).
       2. Uses official Android system trust anchors to prevent MITM attacks.
       3. Allows cleartext only for local developer test environments (localhost / 10.0.2.2 emulator).
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Cleartext permitted exclusively for local development loopback addresses -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
